#!/bin/bash

# Only tests for a successful execution of the program (e.g. without segfault)
# and an exit code of 0. More testing done in unit tests for the corresponding
# viaevo::Program (sub)class(es).
elfs/simple_small
